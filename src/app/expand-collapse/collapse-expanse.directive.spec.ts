import { CollapseExpanseDirective } from './collapse-expanse.directive';

describe('CollapseExpanseDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapseExpanseDirective();
    expect(directive).toBeTruthy();
  });
});
